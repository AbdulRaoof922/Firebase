/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import Emailpasswordauth from './Emailpasswordauth';

AppRegistry.registerComponent(appName, () => Emailpasswordauth);
